how i open jupyter notebook through anaconda prompt
d:
cd python:
conda install python 12
conda activate python 12
pip jupyter notebook
jupyter notebook
then copy link or automatically open in browser

in jupyter notebook


for = iterations loop

why he automatically a space becoz its include in loop in new python its does not required { } braces etc
how we know that its part of loop through his space e.g in for loop it automatically generates 5 space if we write something without sapce of its does't include in part of loop

names: list[str] = ["wak", "alen", "waker"]
for(i=0; i<length(names); i++) {
    names[i]
}